apiVersion: v1
kind: Namespace
metadata:
  name: lunalytics-system
  labels:
    name: lunalytics-system
  annotations:
    # Enable Lunalytics monitoring for this namespace (if using annotation-based filtering)
    lunalytics.io/enabled: "true"
